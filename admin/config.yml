backend:
  name: github
  repo: zhutou369/ws123
  branch: main
  base_url: https://ws-auth-gate.zhutou757.workers.dev
  auth_endpoint: auth

media_folder: "assets/img"
public_folder: "/assets/img"

collections:
  - name: "blog"
    label: "文章发布"
    folder: "posts"
    create: true
    # 强制文件名包含完整时间戳+文章标题，防止任何形式的覆盖
    slug: "{{year}}{{month}}{{day}}-{{hour}}{{minute}}{{second}}-{{slug}}"
    fields:
      - {label: "文章标题", name: "title", widget: "string"}
      - {label: "发布日期", name: "date", widget: "datetime", format: "YYYY-MM-DD HH:mm:ss"}
      - {label: "正文内容", name: "body", widget: "markdown"}
      - {label: "标签", name: "tags", widget: "hidden", default: "blog"}
      - {label: "布局", name: "layout", widget: "hidden", default: "layout.njk"}