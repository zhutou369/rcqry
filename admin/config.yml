backend:
  name: github
  repo: zhutou369/ws123 # 请确保这是你的仓库名
  branch: main
  base_url: https://ws-auth-gate.zhutou757.workers.dev # 你的Worker地址

media_folder: "assets/img"
public_folder: "/assets/img"

collections:
  - name: "blog"
    label: "文章发布"
    folder: "posts"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - {label: "文章标题", name: "title", widget: "string"}
      - {label: "发布日期", name: "date", widget: "datetime", format: "YYYY-MM-DD"}
      - {label: "正文内容", name: "body", widget: "markdown"}
      - {label: "标签", name: "tags", widget: "hidden", default: "blog"}
      - {label: "布局", name: "layout", widget: "hidden", default: "layout.njk"}